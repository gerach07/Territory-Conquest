[build]
builder = "NIXPACKS"

[deploy]
startCommand = "NODE_ENV=production node server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "territory-conquest-server"
